import React from "react";

const NotVisited = (props) => (
    <div className="card">
        <div className="card-body">
            {props.name}
        </div>
    </div>   
);

export default NotVisited;